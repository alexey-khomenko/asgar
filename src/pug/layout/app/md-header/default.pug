div(class="flex justify-center items-center text-white")
    span(class="font-bold text-2xl sm:text-xl")
        | #{page.title.text}
    if page.title.number > 0
        span(class="flex text-lg sm:text-sm h-6 sm:h-5 min-w-6 sm:min-w-5 ml-2")
            span(class="flex justify-center items-center px-1 rounded-full bg-pink flex-grow")
                span(class="font-medium leading-tight")
                    | #{page.title.number}

span(class="h-8 sm:h-6 w-8 sm:w-6")