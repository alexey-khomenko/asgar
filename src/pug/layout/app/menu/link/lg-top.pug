span(class="flex justify-center items-center text-purple-20 h-4 w-4")
    - svg = {width: menu.svg.width, height: menu.svg.height, name: menu.svg.name}
    include ../../../../partials/routes/svg
span(class="font-medium text-xs leading-4 mx-1")
    | #{menu.text}
if menu.number > 0
    span(class="flex text-xs h-4 min-w-4 mr-px")
        span(class="flex justify-center items-center px-1 rounded-full bg-pink flex-grow")
            span(class="font-medium leading-tight")
                | #{menu.number}